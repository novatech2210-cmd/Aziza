; rtp.conf — Sida/AI telephony, office TAS-IX gateway
;
; RTP UDP port range. Office router must allow inbound UDP on this range
; (or rely on conntrack from the outbound RTP flow that the gateway
; initiates first — typical SIP-NAT behaviour).
;
; Source-of-truth: deploy/asterisk-gateway/rtp.conf

[general]
rtpstart = 10000
rtpend = 20000
